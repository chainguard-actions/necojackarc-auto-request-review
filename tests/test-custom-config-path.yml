name: Test custom config path (use_local, missing file)
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          token: ${{ github.token }}
          config: '.github/nonexistent_reviewers_config.yml'
          use_local: 'true'
      - name: Assert action terminated gracefully with custom config path
        run: |
          echo "PASS: action exited 0 (custom config path not found locally, terminated gracefully)"
